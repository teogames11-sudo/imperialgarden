Vertical logo source files for Imperial Garden.

- `imperial-garden_vertical_fill.ai`
- `imperial-garden_vertical_gradient.ai`

Both `.ai` files are PDF-compatible, so matching `.pdf` copies are stored alongside them for easier preview and future export.
The runtime UI uses `../logo-primary.svg` to keep the interface lightweight.
